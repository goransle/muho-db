extends ../layouts/default

block content
	.container: .row: .col-sm-10.col-sm-offset-1.col-md-8.col-md-offset-2
		article
			p: a(href='/galleri') &larr; back to the galleri
			hr
			if !data.post
				h2 Invalid Post.
			else
				header
					h1= data.post.title
					h5 Posted 
						if data.post.publishedDate
							| on #{data.post._.publishedDate.format('MMMM Do, YYYY')} 
						if data.post.categories && data.post.categories.length
							| in 
							each cat, cat_i in data.post.categories
								a(href='/galleri/' + cat.key)= cat.name
								if cat_i < data.post.categories.length - 1
									| ,
								else
						if data.post.author
							| by #{data.post.author.name.first}

						if data.post.content.sources
							p: a(href=data.post.content.sources) Kildenavn
				.post
					if data.post.image.exists
						.image-wrap: img(src="/images/" + data.post.image.filename).img-responsive
					!= data.post.content.full

				hr

				//Skal erstattes med en algoritme som henter nye artikkeltitler, bilder og beskrivelser basert på kategori, ikke tre statiske placeholders
				section#andreArtiklerSection
					p#andreArtiklerTekst Andre artikler
					section
						img(src='http://www.clipartlord.com/wp-content/uploads/2013/01/party-hat2.png' width='150px' alt='placeholder').img-responsive
						p.andreArtiklerTittel: a(href='/galleri') Placeholder text
						img(src='http://images.clipartpanda.com/black-and-white-party-hat-clipart-party-hat-clipart-black-and-white-2.jpg' width='150px' alt='placeholder').img-responsive
						p.andreArtiklerTittel: a(href='/galleri') Placeholder text
						img(src='http://www.clipartbest.com/cliparts/9cR/R5z/9cRR5zEMi.png' width='150px' alt='placeholder').img-responsive
						p.andreArtiklerTittel: a(href='/galleri') Placeholder text


